/*
 * Upstream QuickJS is kept in mechanically split include files so each
 * vendored source object remains manageable for repository tooling. Together
 * these three files are quickjs.c from the commit in README.mintvid, with the
 * documented MintVID portability guards applied.
 */
#include "quickjs_part1.inc"
#include "quickjs_part2.inc"
#include "quickjs_part3.inc"
